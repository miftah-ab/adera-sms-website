Place your real adera-sms.apk file in this folder before deploying.

The download buttons on the site link to /downloads/adera-sms.apk

If you would rather host the APK on GitHub Releases instead of directly on Vercel, replace the href values in components/Hero.tsx and components/FinalCta.tsx with your GitHub Releases URL instead of the local path.
